<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BelajarAPI" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:action">@color/colorPrimary</item>
    </style>

    <color name="search_icon_color">@color/gray_dark</color>

    <style name="Theme.BelajarAPI" parent="Base.Theme.BelajarAPI" />
</resources>