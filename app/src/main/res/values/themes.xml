<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BelajarAPI" parent="Theme.Material3.DayNight">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <color name="icon_color">@color/gray_dark</color>
    <color name="text_color">@color/gray_dark</color>

    <style name="Theme.BelajarAPI.Detail" parent="Theme.BelajarAPI">
        <item name="android:statusBarColor">@color/white_gray</item>
    </style>

    <style name="Theme.BelajarAPI" parent="Base.Theme.BelajarAPI" />
</resources>