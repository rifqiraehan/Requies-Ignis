<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BelajarAPI" parent="Theme.Material3.DayNight">
        <item name="android:statusBarColor">@color/gray_dark</item>
    </style>

    <color name="icon_color">@color/gray_light</color>
    <color name="text_color">@color/white</color>

    <style name="Theme.BelajarAPI.Detail" parent="Theme.BelajarAPI">
        <item name="android:statusBarColor">@color/gray_dark</item>
    </style>
</resources>